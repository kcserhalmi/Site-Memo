name: site_memo
description: Construction field inspection app
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  camera: ^0.10.5+9
  image_picker: ^1.1.2
  speech_to_text: ^6.6.2
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  uuid: ^4.4.2
  intl: ^0.19.0
  share_plus: ^10.0.0
  pdf: ^3.11.0
  archive: ^3.6.0
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.3
  cloud_firestore: ^6.6.0
  sign_in_with_apple: ^8.1.0
  crypto: ^3.0.7
  firebase_storage: ^13.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
